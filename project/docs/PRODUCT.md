# Product

What this project is meant to become, for whom, and in what order.
This is a living plan: edit it as the direction changes, and let git history record how it changed.
How it is meant to be built is in `docs/ARCHITECTURE.md`, what exists today is in `docs/SYSTEMS.md`, and why each feature was built the way it was is in `docs/decisions/`.

Status: TODO(template): "draft" or "reviewed", with the date of the last meaningful update.

## Vision

TODO(template): Two to four sentences on what the product is and the problem it solves. Write only what the user confirms.

## Who it is for

TODO(template): The users and customers, in the order they are served, and why each would use it. Add the reasons behind the order, and the options rejected, because this section is where a later reader looks for them.

## Users and roles

TODO(template): Each kind of user and what they may see and do, or "Not decided yet".

## Roadmap

Phases run in order.
No target dates are recorded: deadlines decide what is cut, never how carefully it is built (see "While you work" in `AGENTS.md`).

### Phase 1

TODO(template): The first release, as a numbered list in build order.

### Later

TODO(template): Direction only, or "None yet".

## Not planned for now

TODO(template): What is deliberately out of scope, so it is not built by accident, or "None yet".

## Data sensitivity and compliance

The risk check in `AGENTS.md` reads this table.
Keep it current as answers arrive, and prefer "Unknown" to a guess.

| Requirement | Applies? | Status | What it typically requires |
|---|---|---|---|
| TODO(template): A law, regulation, contract, or customer review that may apply, one per row, or a single row saying "None known" | | | |

## Open questions

TODO(template): Questions that change what gets built, or "None yet".
